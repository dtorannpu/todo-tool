ktor:
    #application:
    #    modules:
    #        - com.example.ApplicationKt.testModule
    #deployment:
    #    port: 8080
    database:
        driverClassName: org.h2.Driver
        jdbcUrl: "jdbc:h2:mem:test;DATABASE_TO_UPPER=false;MODE=MYSQL"
        username: sa
        password: secret
        maximumPoolSize: 10
